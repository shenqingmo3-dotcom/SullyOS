{
  "name": "sullyos-health-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single-user Apple Health ingest and HTTP MCP on Cloudflare Workers + D1.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dist"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
